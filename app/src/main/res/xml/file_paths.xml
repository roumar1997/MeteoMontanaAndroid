<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Imágenes temporales de "compartir condiciones" -->
    <cache-path name="share" path="share/" />
    <!-- Fotos de celebración del feed (cámara → TakePicture) -->
    <cache-path name="feed" path="feed/" />
</paths>
